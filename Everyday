 Question :  Find the missing number

the sample input  and the output 
Input:
[1, 2, 4, 5, 6]

Output:
3


//solution


// Online Java Compiler
// Use this editor to write, compile and run your Java code online
import java.util.Scanner;
import java.util.Arrays;
class Main {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        System.out.println("can you give me the size of the arrray");
        int n= sc.nextInt();
        int arr[]= new int[n];
        for(int i=0;i<n;i++){
            arr[i]=sc.nextInt();
        }
        System.out.println("array with all the elments we entered"+Arrays.toString(arr));
        
        //the task is to find the missing number the order can be any thing
        for(int i=1;i<n;i++){
            if(arr[i]==arr[0]+i){
                
            }else{
                System.out.println("the missing number"+(arr[0]+i));
                break;
            }
        }
        
    }
}
